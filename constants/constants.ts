const GLOBAL = {
    base_url: process.env.SERVER_URL || "http://localhost",
    accounts_endpoint: "/accounts",
    bills_endpoint: "/bills",
    carts_endpoint: "/carts",
    items_endpoint: "/items",

}